# novel-pro 深度质检报告（发行前最终验收）

- **质检对象**：novel-pro v0.2.3-pro（源目录 `D:\skill开发\5\novel-pro-v0.2.2-pro`）
- **质检时间**：2026-08-02
- **质检方法**：真实逐文件精读 + 跨文件交叉验证（非脚本扫描）。三路深读代理并行覆盖「控制面与流程链 / 角色层与模板层 / 知识库与工具层」，主审亲自精读并逐项验证全部关键争议点（dispatch 派发卡、planning 规划链、自检协议、context-pack 结构、迁移状态机、部署清单、Reader 报告模板等 15+ 处）。
- **质检范围**：SKILL.md、skills/（14）、agents/（12）、knowledge/（102）、templates/（14 md + 3 yaml）、tools/（5 py）、docs/（6）、README.md、skill.json、release.ps1/.bat、.gitignore
- **总体结论**：**未发现严重缺陷，主体高度自洽，0.2.3-pro 发布包不受阻断**。共发现 7 项中等、13 项轻微问题，集中在文档表述与体例层面，建议下一小版本修正。

> **修复状态（2026-08-02 晚）**：M1-M7、L1-L13 已全部修复并复验通过。要点：M4 补写了 `templates/chapters/vol-N-ch-M.md`（章纲 7 字段）与 `templates/acts/volume-N-acts.md`（幕地图）两个缺失模板并注册进 `runtime_manifest.py` + `init.py` 部署清单（运行时条目 138 个）；L12 修 init.py 的 context-pack 题材占位符替换；L8 补 run-log.yaml 条目 schema；额外修复 docs/examples.md 文风示范中的破折号与否定对照（反 AI 纪律）。复验：tools 脚本 compile 通过、`validate_skill_source()` 端到端通过、残留扫描干净、无目标重复。

---

## 一、技能目的与整体框架

### 1.1 目的

novel-pro 是一个**可独立运行的中文长篇小说创作内核**：不依赖任何外部写作平台，Agent Host + 本地项目文件夹即可执行完整创作流程。它的核心命题是——**用结构化的"规划-执行"分工，解决长篇小说创作中单次对话上下文无法容纳全书的问题**：

- 长线连续性由「卷 → 幕 → 章」三层规划承载（每层有独立契约字段，上游字段被下游消费）；
- 阶段理解由「幕级承接快照 + context-pack 预制包」承载（避免重复读取全量知识库）；
- 单章创作注意力由「独立 writer + 单章 Prompt」承载（每章一个自包含生成包）；
- 质量判断由「Reader 冷读 + Anti-AI 扫描 + 整体返修裁决」承载（对真实正文做文学判断，不做关键词门禁）。

### 1.2 整体框架（四层 + 产物目录 + 双模式）

```
┌─────────────────────────────────────────────────────────┐
│ L1 控制面   novel-agent 独占：status/order/run-log/tasks/ │
│             + edit.commit 写入 texts/；17 张派发卡索引    │
├─────────────────────────────────────────────────────────┤
│ L2 流程规则 skills/（14 模块）：规划链、Prompt、写作、     │
│             编辑链、迁移、对齐、TASKS 适配                │
├─────────────────────────────────────────────────────────┤
│ L3 角色执行 agents/（12 角色）：顶层调度 + 3 规划 +        │
│             Prompt 2 + writer + 阅读裁决 4 + 完本 2       │
├─────────────────────────────────────────────────────────┤
│ L4 知识库  knowledge/（102 文件，两层结构）：             │
│            通用底座（webnovel/plot/scene/character/style）│
│           + 类型风格（genre 25 / anti-ai 25+）           │
└─────────────────────────────────────────────────────────┘
  │ 产物：volumes/ acts/ chapters/ prompts/ drafts/ texts/
  │        settings/ .agent/ .migration/
  ▼
主流程：题材初始化 → outline.volume（卷纲+设定+文风确认）
  → outline.act-map/act（幕地图+幕纲）→ outline.chapters（章纲+承接快照）
  → prompt.create（首任务建 context-pack，逐章四节自包含 Prompt）
  → prompts.ready →〔写作模式 write.draft | 编辑模式 edit.write→review→
    anti-ai→synthesize→repair→commit〕→ volume.complete / book.complete
旁路：prompt.review（显式审查）、completion.inspect/revise（完本）、alignment（对齐）、migration.review（迁移）
```

**关键设计约束（贯穿全项目）**：
1. **单真相源**：每个契约只有一个权威文件（17 派发卡 = dispatch.md；章纲 7 字段 = planning.md；文风 = writing-style.md；Prompt 结构 = prompt.md；TASKS schema = templates/TASKS.md；自检协议 = self-contained-prompt.md）；其余文件只引用不重复定义。
2. **所有权边界**：novel-agent 独占控制面与 texts/ 提交；规划角色只写自己的产物；阅读角色（reader/completion-reviewer/prompt-reviewer）只返回报告；writer 零知识隔离。
3. **版本门禁**：runtime_profile=novel-pro-0.2、skill_version=5.2、template_version=0.2.3；旧项目必须走 `migrate.py create→finalize→cleanup` 完整迁移，禁止直接兼容。
4. **反 AI 纪律**：诊断标签/规则名/坏句清单永不进 Prompt，技法只正向溶解；示范文本零破折号、零否定对照、零情绪命名。

---

## 二、分模块深度校验结果

### 2.1 控制面（dispatch.md / SKILL.md / CLAUDE.md / migration.md）

| 校验项 | 结论 |
|---|---|
| 版本门禁触发条件（story.yaml/缺 profile/错 profile/缺 migration 节点） | ✅ 一致（dispatch.md 与 CLAUDE.md 语义一致，体例有 4 vs 5 条之差，见 L3） |
| 迁移状态机（migration.review → finalize → cleanup → resume_step） | ⚠️ 代码行为与 migration.md 状态图顺序表述不符（见 M2） |
| 17 张派发卡（触发/角色/输入/写入/返回/判定/下一跳/恢复八字段） | ✅ 与 docs/interface-reference.md 一一对应；唯一体例差见 M1 |
| 长期 cursor 10 项 vs 临时 order 表 | ✅ 完全一致（outline.acts 为阶段，act-map/act 为其内部 operation，已明示） |
| 所有权边界（novel-agent 独占） | ✅ 全局一致 |
| 恢复逻辑（completed 保留 / running-failed 回 pending / Reader 复读纪律） | ✅ 一致 |
| TASKS.md 适配（pending→确认→in_progress→回写） | ✅ 与 templates/TASKS.md schema 一致 |

### 2.2 内容层

**流程规则（skills/ 14 模块）**
| 校验项 | 结论 |
|---|---|
| 卷纲 8 字段（planning/volume-N/volume-planner/interface-reference 四处） | ✅ 完全一致 |
| 幕纲 11 字段（act-planning/vol-N-act-K/interface-reference 三处） | ✅ 字段名与顺序完全一致 |
| 章纲 7 字段（goal/reader_effect/conflict/characters/scenes/must_hold/ends_with） | ✅ planning.md 与 interface-reference 一致 |
| Prompt 四节 + 场景四件 + 场景权重标注 | ✅ prompt.md/SKILL.md/self-contained-prompt.md/interface-reference 一致 |
| context-pack 8 节 + frontmatter + 第 4 节性质索引 + 第 6 节两层提取 | ✅ 结构一致；第 3 节名称微漂移（见 L4） |
| 编辑模式六步 + 分级分流（REGENERATE/表达→anti-ai） | ✅ SKILL.md/review-archive/CLAUDE.md/interface-reference 四处一致 |
| 自检协议（权威七项 vs 返回六核对点） | ✅ 数字自洽；合并映射未写明（见 L5） |

**角色层（agents/ 12）**
| 校验项 | 结论 |
|---|---|
| 职责边界（谁写/谁只读/谁返回报告） | ✅ 全部自洽，无越权 |
| frontmatter 挂载路径真实性 | ✅ 全部真实存在（唯一例外见 L9） |
| writer 零知识隔离 + novel-base 两部分（构造指南+参考模板） | ✅ 闭环 |
| reader/completion-reviewer 冷读保护（首读不读规划/Prompt/知识） | ✅ cold-read-discipline 三处一致 |
| 角色"只写/不写"表述 | ✅ 语义一致（prompt-crafter 措辞见 L11） |

**模板层（templates/ 14 md + 3 yaml）**
| 校验项 | 结论 |
|---|---|
| 字段链：卷冲突阶梯→幕 conflict_development→章→Prompt | ✅ 四环闭合 |
| 卷信息差弧线→start/end_state→characters 信息差→Prompt 施压点 | ✅ 闭合 |
| 卷承诺清单→幕 promises→章 reader_effect | ✅ 闭合 |
| 幕 emotional_curve→Prompt 情绪线（章纲不重复） | ✅ 闭合，prompt.md 已明示上游 |
| 三套版本号（5.2 / novel-pro-0.2 / 0.2.3） | ✅ 全模板无冲突 |
| author_confirmed 唯一标记 | ⚠️ volume-N.md 有冗余字段（见 M3） |
| .agent 三 yaml 与状态机 | ✅ status/order 对得上；run-log 缺条目 schema（见 L8） |

**知识库（knowledge/ 102）**
| 校验项 | 结论 |
|---|---|
| 两层结构声明与消费顺序（底座先行、类型叠加） | ✅ knowledge/index.md 明文且被执行 |
| 方法计数：plot 13 / scene 16 / style 8 / character 3 / genre 25 | ✅ index 与实际文件一一对应 |
| genre 注册表 25 ↔ genre/ 25 文件 ↔ anti-ai/genre 25 文件 | ✅ 三方对应（cross-genre 为 index 声明的跨题材融合） |
| self-contained-prompt.md 七项自检内容 | ✅ 与 context-pack 模板"自检七步法"呼应 |
| 索引"问题→文件"映射抽样 | ✅ 抽查 5 处均与实际内容对应 |

### 2.3 工具层（tools/ 5 + 发布链 + docs/ 6）

| 校验项 | 结论 |
|---|---|
| init.py REQUIRED_SOURCE_FILES（24 项）+ runtime_entries 部署集 | ✅ 与实际源文件逐一对应，无有清单无文件/有文件无清单 |
| runtime_manifest SUBDIR_TEMPLATE_FILES（volumes/acts/characters/根 TASKS） | ✅ 与模板目录完全对应 |
| migrate.py REQUIRED_MIGRATION_FIELDS（11 字段）vs status.yaml migration 块 | ✅ 逐字段一致 |
| migrate.py 子命令参数（create <旧><新> / finalize <新> / cleanup <新> --confirm） | ✅ 与 SKILL.md/migration.md/CLAUDE.md/docs 一致 |
| 编辑模式六步（editing-mode-guide vs review-archive） | ⚠️ Reader 报告模板缺两节（见 M7） |
| README 创作主线 vs SKILL.md 门面 | ✅ 一致 |
| release.ps1 排除规则（.zcode 等） | ✅ 已生效（zip 实测无残留） |

---

## 三、问题清单

### 无【严重】

主链路（规划→Prompt→写作→编辑→完本）、所有权边界、迁移命令、部署清单均自洽，未发现任何会阻断使用或导致运行错误的断裂。

### 【中等】7 项（建议 0.2.3.x 定稿前修正）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| M1 | `skills/dispatch.md:76` vs `skills/planning.md:11` | outline.volume 卡「下一跳」写长期阶段名 `outline.acts`，其余卡与 planning 链写 operation 名（`outline.act-map`），体例不一，易误读为冲突 | dispatch.md:76 改为 `outline.act-map` |
| M2 | `skills/migration.md:48` | 状态图把「cursor.step 恢复为 resume_step」画在 cleanup 之后；实际 migrate.py finalize（:753-756）已恢复，cleanup 只置 cleanup=complete（:808） | 把恢复行移到 finalize 之后或注明「finalize 已恢复」 |
| M3 | `templates/volumes/volume-N.md:5` | frontmatter 含 `author_confirmed: false`，但消费方（prompt-crafter/prompt.md/planning）只读 story.md 的该标记；存在第二套确认源风险 | 删除该字段或注明仅文档性 |
| M4 | `docs/templates-guide.md:37/530/627` | 把 `chapters/vol-N-ch-M.md`、`acts/volume-N-acts.md` 当模板引用，但 templates/ 无此二文件（章纲 7 字段 schema 内联在 planning.md） | 补章纲/幕地图模板并注册，或 guide 注明「schema 权威在 skill」 |
| M5 | `docs/templates-guide.md:144-148` | 卷纲示例把「本卷目标与失败代价」拆成「本卷目标」「失败代价」两个 ##，与 volume-N.md 单节不符 | 示例与模板统一为单节 |
| M6 | `docs/templates-guide.md:257-261` | 字段链表使用「章目标与阻力」「章内信息差」等非章纲 7 字段名 | 改用 goal/conflict/characters 等真实字段名 |
| M7 | `docs/editing-mode-guide.md:98-116` | Reader 冷读报告模板缺 review-archive.md:108-113 的「最终复读」「接受候选」两节，按 guide 填写会缺 edit.commit 依赖字段 | 补齐两节或注明「完整字段以 review-archive.md 为准」 |

### 【轻微】13 项

| # | 位置 | 问题 |
|---|---|---|
| L1 | `docs/framework-overview.md:34` | knowledge「7 主题，97 文件」过时，实际 102 个 .md |
| L2 | `docs/framework-overview.md:85` | style「9 文风原型」应为 8 原型（9 文件含 index）；character「(4)」按文件数、interface-reference「3 方法」按方法数，口径不一 |
| L3 | `skills/dispatch.md:11-13` vs `templates/CLAUDE.md:64-69` | 版本门禁触发条件清单 4 条 vs 5 条（migration.review 位置不同），语义一致、体例不一 |
| L4 | context-pack 第 3 节名称 | `skills/context-pack.md:20`「冲突钩点节奏」/ 模板:26「冲突、钩点与节奏方法」/ docs:501「冲突钩点与节奏方法」，三处微漂移 |
| L5 | `skills/prompt.md:139` vs `self-contained-prompt.md:129-152` | 返回六核对点表头与权威七项合并映射未写明（新人测试/引用扫描/状态具体性未在表头显式体现），防后续漂移应补一行映射说明 |
| L6 | `docs/interface-reference.md:96` | edit.review 完成判定丢了「无未解决问题或已分流」的容错语义（dispatch.md:163 有） |
| L7 | `docs/templates-guide.md:49-52` | 称所有模板含 template_version/required_fields，实际 settings/ 下 6 个无 frontmatter、volume-N 无 template_version |
| L8 | `templates/.agent/run-log.yaml` | 仅 `runs: []` 无条目字段 schema，SKILL.md/CLAUDE.md 仅述用途 |
| L9 | `agents/novel-agent.md:16` | knowledge 挂载 `.agent/status.yaml`（运行时路径）与其余 agent 挂载源码路径约定不同，例外未在 agent-return-spec.md:21 注明 |
| L10 | `templates/runtime/novel-base.md:98-106` | 项目级声线硬规则（禁破折号/否定对照/超短句）写死于模板，与 writing-style.md:43 声线禁区为两套来源，若项目禁区冲突时优先级未写明（:98「唯一指令源」与「还必须遵守硬规则」并列有张力） |
| L11 | `agents/prompt-crafter.md:32` | 「不写……设定」与「另写 settings/context-pack.md」字面并列，建议区分创作设定与预制包 |
| L12 | `tools/init.py:184-187` | context-pack.md 用 copy_if_missing 复制，frontmatter 的 `volume: {N}`、`genre_id: {题材编号}` 未随题材替换（与 story.md 的 {{...}} 处理不同；设计上由首任务重写，仍建议 init 时替换） |
| L13 | `docs/examples.md:23` | 示例题材 `urban-xianxia` 不在注册表（实为 urban-cultivation）；:428-471 文风示例缺「稳定与变化」节 |

---

## 四、总体结论与定稿建议

1. **发布判定**：0.2.3-pro 主体高度自洽，17 张派发卡、三层字段链、状态机、所有权边界、部署清单、迁移契约全部真实可用且相互一致；**不存在阻断发行的严重问题**。
2. **建议下一小版本（0.2.3.x）优先处理 M1-M3**（体例冲突、状态图顺序、冗余确认字段）——这三项直接影响后续维护者按文档操作的正确性。
3. **M4-M7 与 L1-L13 均为文档表述/体例层**，不影响运行时行为，可在文档维护时批量收敛；其中 L5（自检映射说明）建议顺手补上，防止 schema 漂移。
4. **本报告为开发过程产物**，会被下一次 release.ps1 打进发布包；若不需要随包发布，请在打包前移出或加入 release.ps1 排除列表。
